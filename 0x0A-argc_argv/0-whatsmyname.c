#include <stdio.h>


/**
 * main - checks code
 *@argc: number of arguments passed in
 *@argv: pointer to the character array with all strings passed
 * Return: 0 on success.
 */
int main(int argc, char **argv)
{
	/*name(argv);*/
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
