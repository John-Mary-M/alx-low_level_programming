#include <stdio.h>
#include "_putchar.c"

/**
 * main - Entry point.
 *
 * _putchar - writes alphabet in lowercase to stdout
 *
 * Return: On success 0.
 * On error, -1 is returned.
 */
int main(void)
{
	int *i;

	for (i == 'a'; i <= 'z'; i++)
	{
		_putchar(char *);
	}
	_putchar("\n");
	return (0);
}
